---
scope: Centre de contrôle Proxmox-like (spec-proxmox-inspired-fleet, 9 stories)
purpose: Spine de build pour agents BMad + doc lisible + deck de présentation
altitude: feature→epics/stories
updated: 2026-08-23T14:40
---

- (decision) [ADOPTED] Paradigme : bibliothèque cœur HomePortKit + frontends minces (CLI hpm, app SwiftUI) — toute logique métier dans la lib, jamais dans un frontend
- (decision) [ADOPTED] Pattern HomePortKit : opérations en extensions Manager+<Domaine> ; effets isolés dans SSHClient/ProcessRunner ; FleetStore possède fleet.yaml ; ReleaseService possède le cache GitHub
- (decision) [ADOPTED] App : SwiftUI, FleetModel @MainActor unique source d'état UI, refresh périodique en TaskGroup, notifications via Notifier sur transitions
- (version) Package.swift : swift-tools 5.9, macOS 13+, swift-argument-parser >=1.3, Yams >=5.0
- (event) Balayage brownfield : Sources/HomePortKit (13 fichiers), Sources/hpm, App/Sources (5 fichiers) — conventions lisibles et cohérentes
- (decision) API Homeport atteinte en HTTP direct via Tailscale (même serveur que le dashboard) — cohérent avec la WebView CAP-3 ; le healthz de diagnostic reste vérifié localement via SSH
- (decision) Événements en pull avec curseur : Homeport garde l'historique local, le Mac interroge events?since=<curseur> (~30-60 s) — rattrapage naturel après fermeture ; stream = optimisation future explicitement différée
- (decision) Découverte de capacités : endpoint capabilities (version API + features events/metrics) ; 404 = Homeport pas à niveau → affichage dégradé sans erreur
- (direction) Vincent : prévoir une intégration formalisée entre le projet Homeport et HomePortManager (mécanisme de partage du contrat API à définir)
- (decision) Intégration inter-repos validée : contrat API = document versionné, source de vérité dans le repo Homeport, copie épinglée docs/api/ dans HomePortManager ; capabilities renvoie la version du contrat, hpm déclare sa plage supportée ; story-spec 6-7 rédige la v1 du contrat
- (decision) Propriété des données : un seul owner par donnée — métriques+événements = le Pi (servis par l'API, rien de durable côté Mac) ; journal des tâches = le Mac ; backups = archives des deux côtés, rotations existantes
- (decision) Stockage central Mac : SQLite (WAL) dans ~/.local/state/hpm/hpm.db (convention XDG existante), écrit exclusivement via HomePortKit — jamais par un frontend en direct
- (decision) Rétention métriques côté Pi : multi-échelles façon RRD — 24h@1min, 7j@5min, 30j@1h, 1an@1j — stockage borné en SQLite côté Homeport
- (decision) Backups planifiés : timer+units systemd déployés par hpm (a) ; le script invoque la fonction de backup de Homeport si la version installée l'expose (b), sinon fallback sur le script générique hpm — [ASSUMPTION] synthèse a+b à confirmer par Vincent
- (decision) Consolidation : opportuniste à chaque refresh de flotte (archives non rapatriées → scp, rotation 10, résultat au journal) + explicite via hpm backup sync / bouton
- (decision) Définition des jobs : le Mac possède l'état désiré (config hpm) ; hpm apply le synchronise de façon idempotente sur le Pi (units + copie de config locale au Pi) ; le Pi tourne en autonomie ; un écart détecté = warning doctor
- (decision) Fenêtre : même process que la menubar, scène Window « Centre de contrôle » en NavigationSplitView — sidebar flotte/machines, détail en onglets (Résumé, Logs, Événements, Métriques, Backups, Shell, Updates)
- (decision) Shell intégré : SwiftTerm embarqué dans la fenêtre (version et maturité à vérifier au web avant d'engager) ; Terminal.app en repli si la lib déçoit
- (decision) Concurrence : une seule action mutante à la fois par machine — file par machine dans HomePortKit, partagée CLI+app ; lectures libres et parallèles ; toute action mutante atterrit au journal des tâches
- (decision) Parité CLI systématique, CLI d'abord : chaque capacité naît dans HomePortKit, s'expose en CLI (hpm events, tasks, backup jobs, metrics…) puis dans l'app
- (note) Q7 synthèse a+b non contredite par Vincent (acceptation tacite) — l'ASSUMPTION reste taguée pour le triage final
- (decision) Sécurité API : le tailnet est l'authentification (comme le dashboard aujourd'hui) ; pas d'auth applicative en v1
- (direction) Différé avec condition de reprise : ajouter un token applicatif (défense en profondeur) plus tard — à revisiter si la flotte s'ouvre au-delà du tailnet personnel ou à l'arrivée de la console web
- (version) SwiftTerm vérifié au web 2026-08-23 : actif (migueldeicaza/SwiftTerm), TerminalView AppKit + NSViewRepresentable, SPM
- (assumption) Accès SQLite via l'API C système (pas de dépendance ORM type GRDB) — cohérent avec la philosophie zéro-dépendance du projet, à confirmer au triage
- (decision) Graphes de métriques : Swift Charts natif (macOS 13+, déjà dans la cible de déploiement) — pas de lib de charting tierce
- (event) Gate de revue : lint 0 finding ; 4 relecteurs parallèles (rubric, vérification web, adversarial, réconciliation) — 0 contradiction, 5 findings critical/high concurrence temporelle + ATS + shell + versions
- (decision) AD-16 ajouté : jobs du timer Pi = événements Homeport, le journal Mac ne consigne que les actions initiées par le Mac ; flock partagé côté Pi entre timer et actions hpm (fix F1 adversarial)
- (decision) AD-5 resserré : curseur (epoch, id) — reset/restore Pi incrémente l'epoch ; curseur de lecture et notified_up_to distincts ; décision de notifier dans le Kit (fix F2+F4)
- (decision) AD-12 resserré : verrou inter-process persistant dans hpm.db (pas de file en RAM) + verrou flock côté Pi (fix F3)
- (decision) AD-9/AD-11 resserrés : script Pi root autonome (data dir local, mqtt.env), archives atomiques tmp+mv, consolidation single-flight, précondition sudo vérifiée par doctor avant déploiement (fix F5 + NL-2 + NL-6)
- (decision) AD-3 resserré : exception ATS unique dans Info.plist partagée URLSession+WKWebView ; HTTPS tailscale cert différé (fix H-2/F-1)
- (decision) AD-17 ajouté : shell = canal d'évasion assumé — pas de verrou, pas de journal détaillé, identité SSH depuis fleet.yaml, une entrée informative à l'ouverture (fix H-1)
- (version) SwiftTerm épinglé 1.19.0 (dernière stable 1.x, la 2.0 exige tools 6.2) ; LocalProcessTerminalView + /usr/bin/ssh ; Package.resolved fait foi ; sap 1.8+ = Swift 6, ne monter qu'avec la toolchain (fix F-2/F-4/F-5)
- (decision) Conventions ajoutées : identité SSH de fleet.yaml pour tous canaux (NL-1) ; confirmations destructives partout (NL-4) ; politique de notification unifiée API-events sinon fallback transitions (NL-3/NL-5/M-2) ; 3 états API distincts ; busy_timeout + migrations user_version + rétention journal bornée (F-3/M-1/M-3) ; owner schéma = story 5, owner contrat = story 6
- (event) spine finalized — status final, adopté comme companion du spec ; livrables : SOLUTION-DESIGN.md + deck.html (artifact publié)
- (note) Polish doc_standards (bmad-review structure/prose) non exécuté en passe séparée — écriture soignée directe, dérogation signalée à Vincent
- (decision) Rebranding : « Proxmox-like » retiré du spine, du solution design et du deck — nom retenu « centre de contrôle unifié » ; les invariants ne changent pas, deck republié même URL
- (decision) AD-12 amendé (ID stable) après revue party mode des stories : verrou porteur de PID+horodatage, révocable (process mort ou TTL 30 min), tâche close en interrupted ; hpm unlock refuse tant que le détenteur est vivant
- (event) Spine mis à jour post-finalisation — trou de conception (verrou orphelin) trouvé par la revue des stories, corrigé à la source
